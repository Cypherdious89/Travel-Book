<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Place</title>
</head>

<body>
    <form action="/sites/<%= site._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="site[title]" value="<%= site.title %>">
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="site[location]" value="<%= site.location %>">
        </div>
        <div>
            <button>Update Place !</button>
        </div>
    </form>
    <a href="/sites/<%= site._id %>">Back</a>
</body>
</html>